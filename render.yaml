services:
  - type: web
    name: ai-detector-api
    plan: free
    env: docker
    autoDeploy: true
    healthCheckPath: /health
    envVars:
      - key: AIDETECTOR_DEVICE
        value: cpu
      - key: AIDETECTOR_MODE_PROFILE
        value: balanced
